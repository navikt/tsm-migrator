ktor:
  environment: local
  application:
    modules:
      - no.nav.tsm.ApplicationKt.module
  deployment:
    port: 8080
  kafka:
    config:
      bootstrap.servers: localhost:9092
      security.protocol: "PLAINTEXT"

app:
  name: migrator-local
